Bubble ==> When an event happens on an element, it first runs the handlers on it, then on its parent,
           then all the way up on other ancestors.

Capture ==> The event first goes through the ancestors chain down to the element until
            it reaches the target
